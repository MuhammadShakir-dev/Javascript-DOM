// lets have a look on how to change styles of html elements using javascript.

//js

const h1 = doucment.querySelectr('h1');
h1.style.fontWeigth = 'bold';

const btn = document.querySelector('button');
btn.style.fontSize = '25px';
btn.style.fontFamily = 'Opensans';

